# CampusRank Setup and Run Plan

## Steps:
- [x] Step 1: Install dependencies (`npm install`)
- [x] Step 2: Create .env.local with GEMINI_API_KEY (configured in .env)
- [x] Step 3: Run dev server (`npm run dev`)
- [x] Step 4: Confirm server running and browser opens (verified at http://localhost:3001)
- [x] Step 5: GitHub Copilot manual install/enable in VSCode (Extensions view)

**Status:** Completed all setup steps. Git repository is synchronized with origin/main.


